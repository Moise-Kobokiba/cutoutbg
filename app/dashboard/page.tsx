import type { Metadata } from "next"
import { WorkspaceShell } from "@/components/workspace-shell"
export const metadata: Metadata = { title: "Dashboard — CutoutBG", description: "Your future CutoutBG workspace." }
export default function DashboardPage() { return <WorkspaceShell title="Overview"><section className="mt-8 rounded-3xl border border-dashed border-[var(--line)] bg-[var(--panel)] p-8 sm:p-12"><div className="max-w-xl"><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--muted)]">Prototype workspace</p><h2 className="mt-4 font-display text-3xl font-bold tracking-[-.04em]">Your workspace will appear here once accounts are connected.</h2><p className="mt-4 leading-7 text-[var(--muted)]">There is no processing history, usage count, credit balance, or user data to show yet.</p></div></section><div className="mt-6 grid gap-4 sm:grid-cols-2"><div id="history" className="rounded-3xl border border-[var(--line)] bg-[var(--panel)] p-6"><h2 className="font-display text-xl font-bold">History</h2><p className="mt-2 text-sm text-[var(--muted)]">No saved jobs yet.</p></div><div id="usage" className="rounded-3xl border border-[var(--line)] bg-[var(--panel)] p-6"><h2 className="font-display text-xl font-bold">Usage</h2><p className="mt-2 text-sm text-[var(--muted)]">Usage tracking is coming with accounts.</p></div></div></WorkspaceShell> }
