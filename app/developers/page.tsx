import type { Metadata } from "next"
import Link from "next/link"
import { ArrowRight, Code2 } from "lucide-react"
import { SiteHeader } from "@/components/site-header"
import { ProductFooter } from "@/components/product-footer"
export const metadata: Metadata = { title: "CutoutBG API — Developers", description: "Learn about the upcoming CutoutBG developer API." }
const steps = [["Upload image", "Send an image to the future processing endpoint."], ["Process image", "Choose the output behavior your workflow needs."], ["Retrieve result", "Receive a transparent image when processing completes."]]
export default function DevelopersPage() { return <><SiteHeader /><main className="mx-auto max-w-[1100px] px-5 pb-20 pt-16 lg:px-8 lg:pt-24"><div className="max-w-3xl"><div className="flex size-12 items-center justify-center rounded-2xl bg-[var(--accent)] text-[var(--accent-ink)]"><Code2 aria-hidden="true" /></div><p className="mt-8 text-xs font-bold uppercase tracking-[.16em] text-[var(--muted)]">Developers · Coming soon</p><h1 className="mt-4 font-display text-5xl font-bold tracking-[-.06em] sm:text-6xl">CutoutBG for developers</h1><p className="mt-5 text-lg leading-8 text-[var(--muted)]">Integrate background removal into your own applications. The API is not available yet, but this is the shape we&apos;re working toward.</p></div><div className="mt-12 grid gap-4 md:grid-cols-3">{steps.map(([title, copy], index) => <div key={title} className="rounded-3xl border border-[var(--line)] bg-[var(--panel)] p-6"><span className="text-sm font-bold text-[var(--muted)]">0{index + 1}</span><h2 className="mt-8 font-display text-xl font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{copy}</p></div>)}</div><section className="mt-10 rounded-3xl bg-[var(--deep)] p-7 text-white"><p className="text-xs font-bold uppercase tracking-[.16em] text-white/60">Upcoming API contract</p><pre className="mt-5 overflow-x-auto rounded-2xl bg-black/20 p-5 text-sm leading-7 text-white/80"><code>{`POST /v1/remove-background\nContent-Type: multipart/form-data\n\nimage: <file>`}</code></pre><p className="mt-5 text-sm text-white/70">API keys, usage, documentation, and executable endpoints will be added only after the service is ready.</p></section><Link href="/remove-background" className="mt-8 inline-flex items-center gap-2 text-sm font-bold">Try the public tool instead <ArrowRight className="size-4" aria-hidden="true" /></Link></main><ProductFooter /></> }
