"use client"
import { useState } from "react"

export function BeforeAfterViewer({ imageUrl }: { imageUrl: string }) { const [position, setPosition] = useState(52); return <div className="relative aspect-[4/3] overflow-hidden rounded-[24px] bg-[#dfe9e2]" onPointerMove={(event) => { if (event.buttons === 1) { const rect = event.currentTarget.getBoundingClientRect(); setPosition(Math.max(4, Math.min(96, ((event.clientX - rect.left) / rect.width) * 100))) } }}><img src={imageUrl} alt="Original image with its background" className="absolute inset-0 size-full object-cover" /><div className="checkerboard absolute inset-y-0 right-0 overflow-hidden" style={{ left: `${position}%` }}><img src={imageUrl} alt="Background removed preview" className="absolute inset-y-0 right-0 h-full max-w-none object-cover" style={{ width: `${100 / (1 - position / 100)}%` }} /></div><div className="absolute inset-y-0 w-0.5 bg-white shadow-[0_0_0_1px_rgba(19,35,30,.18)]" style={{ left: `${position}%` }}><button type="button" aria-label="Drag to compare original and background removed image" className="absolute left-1/2 top-1/2 flex size-10 -translate-x-1/2 -translate-y-1/2 items-center justify-center rounded-full border-2 border-white bg-[#17332b] text-white shadow-lg" onPointerDown={(event) => { event.currentTarget.setPointerCapture(event.pointerId) }}><span className="text-lg">↔</span></button></div><span className="absolute left-3 top-3 rounded-full bg-white/90 px-3 py-1.5 text-xs font-semibold text-[#17332b]">Original</span><span className="absolute right-3 top-3 rounded-full bg-[#17332b]/90 px-3 py-1.5 text-xs font-semibold text-white">Background removed</span></div> }
