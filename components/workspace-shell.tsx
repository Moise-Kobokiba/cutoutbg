import Link from "next/link"
import { ChartNoAxesColumn, History, ImageMinus, LayoutDashboard, Settings, User } from "lucide-react"
import { SiteHeader } from "@/components/site-header"
const nav = [["Dashboard", "/dashboard", LayoutDashboard], ["Remove Background", "/remove-background", ImageMinus], ["History", "/dashboard#history", History], ["Usage", "/dashboard#usage", ChartNoAxesColumn], ["Account", "/account", User], ["Settings", "/account#settings", Settings]] as const
export function WorkspaceShell({ children, title }: { children: React.ReactNode; title: string }) { return <><SiteHeader /><div className="mx-auto grid max-w-[1240px] gap-8 px-5 pb-24 pt-8 lg:grid-cols-[230px_1fr] lg:px-8"><aside className="rounded-3xl border border-[var(--line)] bg-[var(--panel)] p-3 lg:h-fit"><p className="px-3 py-3 text-xs font-bold uppercase tracking-[.16em] text-[var(--muted)]">Your workspace</p><nav className="flex gap-1 overflow-x-auto lg:flex-col" aria-label="Workspace navigation">{nav.map(([label, href, Icon]) => <Link key={label} href={href} className="flex shrink-0 items-center gap-3 rounded-2xl px-3 py-3 text-sm font-semibold text-[var(--muted)] hover:bg-[var(--soft)] hover:text-[var(--foreground)]"><Icon className="size-4" aria-hidden="true" />{label}</Link>)}</nav></aside><main><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--muted)]">Workspace</p><h1 className="mt-3 font-display text-4xl font-bold tracking-[-.05em]">{title}</h1>{children}</main></div></> }
