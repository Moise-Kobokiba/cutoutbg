"use client"
import { FormEvent, useState } from "react"
export function PrototypeForm({ kind }: { kind: "login" | "signup" }) { const [message, setMessage] = useState(""); const submit = (event: FormEvent) => { event.preventDefault(); setMessage(kind === "login" ? "Authentication coming soon." : "Account creation is coming soon.") }; return <form onSubmit={submit} className="flex flex-col gap-5"><div className="flex flex-col gap-2"><label htmlFor="name" className="text-sm font-semibold">{kind === "signup" ? "Name" : "Email"}</label><input id="name" name="name" type={kind === "signup" ? "text" : "email"} required className="rounded-2xl border border-[var(--line)] bg-[var(--panel)] px-4 py-3 outline-none focus:border-[var(--deep)]" /></div>{kind === "signup" && <div className="flex flex-col gap-2"><label htmlFor="email" className="text-sm font-semibold">Email</label><input id="email" name="email" type="email" required className="rounded-2xl border border-[var(--line)] bg-[var(--panel)] px-4 py-3 outline-none focus:border-[var(--deep)]" /></div>}<div className="flex flex-col gap-2"><label htmlFor="password" className="text-sm font-semibold">Password</label><input id="password" name="password" type="password" required className="rounded-2xl border border-[var(--line)] bg-[var(--panel)] px-4 py-3 outline-none focus:border-[var(--deep)]" /></div>{kind === "signup" && <div className="flex flex-col gap-2"><label htmlFor="confirm" className="text-sm font-semibold">Confirm password</label><input id="confirm" name="confirm" type="password" required className="rounded-2xl border border-[var(--line)] bg-[var(--panel)] px-4 py-3 outline-none focus:border-[var(--deep)]" /></div>}<button type="submit" className="rounded-full bg-[var(--deep)] px-5 py-3.5 text-sm font-bold text-white">{kind === "login" ? "Log in" : "Create account"}</button>{message && <p role="status" className="rounded-2xl border border-[var(--line)] bg-[var(--soft)] px-4 py-3 text-sm text-[var(--muted)]">{message}</p>}</form> }
